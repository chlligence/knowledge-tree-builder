# 📐 Task Visualizer — 任务可视化工具

> 一款极简的 Web 端任务可视化工具。在左侧编辑器中书写 Markdown 格式的任务清单，右侧自动实时渲染为交互式树状图。

---

## 目录

- [快速开始](#快速开始)
- [界面概览](#界面概览)
- [书写语法](#书写语法)
- [自定义标签系统](#自定义标签系统)
- [编辑器快捷键](#编辑器快捷键)
- [可视化交互](#可视化交互)
- [数据存储](#数据存储)
- [技术架构](#技术架构)
- [项目文件结构](#项目文件结构)
- [本地运行](#本地运行)

---

## 快速开始

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

打开浏览器访问 `http://localhost:5173/` 即可使用。

首次打开时，编辑器会自动填充一组示例数据，展示所有解析功能。

---

## 界面概览

应用采用 **50/50 分屏布局**：

```
┌──────────────────────────────────────────────────┐
│  Task Visualizer                  Saved · 1/9 tasks │
├───────────────────────┬──────────────────────────┤
│  EDITOR               │  TREE VIEW    - 100% +  ⤢ │
│                       │                          │
│  - [ ] 任务A          │       ○── 任务A           │
│    - [x] 子任务1      │       │  ├── ● 子任务1     │
│    - [ ] 子任务2      │       │  └── ○ 子任务2     │
│  - [ ] 任务B          │       ○── 任务B           │
│                       │                          │
├───────────────────────┤                          │
│  ● 1  ○ 8             │                          │
└───────────────────────┴──────────────────────────┘
```

| 区域 | 说明 |
|------|------|
| **顶部导航栏** | 显示应用名称、保存状态（绿色呼吸灯 = 已保存）、任务完成计数 |
| **左侧 · 编辑器** | 纯文本输入区，支持 Markdown 语法高亮提示和快捷键 |
| **中间 · 分隔条** | 可拖拽调整左右面板宽度（范围 20%–80%） |
| **右侧 · 树状图** | D3.js 渲染的交互式 SVG 树，支持缩放和平移 |

---

## 书写语法

### 基本格式

每个任务项是一行标准的 Markdown 无序列表 + 复选框：

```markdown
- [ ] 未完成的任务
- [x] 已完成的任务
```

### 层级关系

通过 **2 个空格** 缩进表示父子关系：

```markdown
- [ ] 一级任务
  - [ ] 二级子任务
    - [ ] 三级子任务
  - [x] 另一个二级子任务
```

渲染效果：

```
一级任务
├── 二级子任务
│   └── 三级子任务
└── ✓ 另一个二级子任务
```

### 多棵树

顶级（无缩进）的任务项会各自成为独立的分支，统一挂在虚拟根节点下：

```markdown
- [ ] 项目 A
  - [ ] 子任务
- [ ] 项目 B
  - [ ] 子任务
```

---

## 自定义标签系统

在任务文本后添加 `#key:value` 格式的标签，可以控制节点的视觉样式。标签不会显示在节点文字中。

### `#tag:标签名` — 分类标签

在节点旁显示一个彩色徽章：

```markdown
- [ ] 配置 CI/CD 流水线 #tag:DevOps
- [ ] 训练 PPO 模型 #tag:RL
```

> 已预设配色的标签名：`DevOps`（靛蓝）、`RL`（粉红）。其他标签名使用默认灰色配色。

### `#color:颜色` — 自定义节点颜色

覆盖节点默认颜色。支持命名颜色或十六进制色值：

```markdown
- [x] 已完成且标绿 #color:green
- [ ] 自定义紫色 #color:purple
- [ ] 十六进制色 #color:#ff6600
```

**支持的命名颜色：**

| 颜色名 | 色值 | 预览 |
|--------|------|------|
| `red` | `#ff3b30` | 🔴 |
| `green` / `emerald` | `#34c759` | 🟢 |
| `blue` | `#0071e3` | 🔵 |
| `amber` / `orange` | `#ff9f0a` | 🟠 |
| `purple` | `#af52de` | 🟣 |
| `pink` | `#ff2d55` | 粉 |
| `teal` | `#5ac8fa` | 青 |
| `` | `#5856d6` | 靛 |
| `cyan` | `#32d2e2` | 氰 |
| `yellow` | `#ffcc00` | 🟡 |

### `#status:状态` — 状态标记

在节点右上角显示一个小型状态图标：

```markdown
- [ ] 进行中的任务 #status:active
- [ ] 被阻塞的任务 #status:blocked
```

| 状态值 | 图标 | 颜色 | 含义 |
|--------|------|------|------|
| `active` | ● | 蓝色 | 进行中 |
| `blocked` | ⚠ | 琥珀色 | 被阻塞/等待 |
| `error` | ✕ | 红色 | 出错 |

### `#priority:high` — 高优先级

节点获得加粗边框 + 发光效果：

```markdown
- [ ] 紧急修复线上 Bug #priority:high
```

### 组合使用

标签可以自由组合：

```markdown
- [ ] 关键部署任务 #tag:DevOps #priority:high #color:red
```

---

## 编辑器快捷键

### Enter — 智能续行

在一行任务项末尾按 `Enter`，自动换行并继承当前缩进层级 + 生成新的 `- [ ] `：

```
输入: - [ ] 第一个任务⏎
结果: - [ ] 第一个任务
      - [ ] |            ← 光标在此，自动补全
```

子任务同理，缩进自动保持：

```
输入:   - [ ] 子任务A⏎
结果:   - [ ] 子任务A
        - [ ] |          ← 保持 2 格缩进
```

**退出续行：** 如果在一个空的 `- [ ] ` 行上按 `Enter`，该空行会被自动清除。

### Tab — 增加缩进

按 `Tab` 将当前行增加 2 格缩进，使其变为上方任务的子任务：

```
按 Tab 前: - [ ] 任务A
按 Tab 后:   - [ ] 任务A    ← 变成子任务
```

### Shift + Tab — 减少缩进

按 `Shift + Tab` 将当前行减少 2 格缩进，提升层级：

```
按 Shift+Tab 前:   - [ ] 子任务
按 Shift+Tab 后: - [ ] 子任务    ← 提升为顶级任务
```

---

## 可视化交互

### 节点样式

| 类型 | 外观 |
|------|------|
| 未完成任务 `[ ]` | 空心圆 ○（灰色描边） |
| 已完成任务 `[x]` | 实心圆 ●（蓝色填充 + ✓） |
| 连接线 | 细灰色曲线；已完成分支为淡绿色 |

### 缩放控制

右侧面板工具栏提供 3 个按钮：

| 按钮 | 功能 |
|------|------|
| **−** | 缩小视图 |
| **+** | 放大视图 |
| **⤢** | 自适应视图（将树完整居中显示） |

中间显示当前缩放百分比（如 `100%`）。

### 鼠标操作

| 操作 | 效果 |
|------|------|
| **滚轮** | 缩放树状图（0.2x – 3x） |
| **左键拖拽** | 平移画布 |

### 分隔条拖拽

鼠标拖拽中间的分隔线可以调整编辑器与可视化面板的宽度比例。

---

## 数据存储

- 所有数据保存在浏览器的 **localStorage** 中，键名为 `task-viz-markdown`
- 每次文本变更后 **300ms 自动保存**（防抖）
- 页面刷新或重新打开后自动恢复上次的内容
- 首次使用时自动加载示例数据
- **无需后端**，所有数据均在本地

> ⚠️ 清除浏览器数据会导致任务丢失。如需备份，请直接复制编辑器中的文本。

---

## 技术架构

```
Vue 3 (Composition API + Script Setup)
├── App.vue             主组件：布局、状态、快捷键
├── parser.js           Markdown 解析器
├── useTreeRenderer.js  D3.js 树渲染组合式函数
├── defaults.js         默认示例数据
└── style.css           设计系统（CSS 自定义属性）
```

**数据流（严格单向）：**

```
文本输入 → 防抖 300ms → parser.js 解析 → JSON 树结构 → D3.js 渲染 SVG
                    ↘ localStorage 保存
```

**核心依赖：**

| 依赖 | 版本 | 用途 |
|------|------|------|
| Vue 3 | ^3.x | 响应式 UI 框架 |
| D3.js | ^7.x | SVG 树状图渲染、缩放平移 |
| Vite | ^8.x | 开发服务器与构建工具 |

---

## 项目文件结构

```
任务可视化/
├── index.html              入口 HTML（含 Inter 字体、SEO meta）
├── package.json            项目配置与依赖
├── vite.config.js          Vite 配置
├── 文档说明.md              本文档
├── public/
│   └── vite.svg            favicon
└── src/
    ├── main.js             Vue 应用入口
    ├── App.vue             主组件（布局 + 编辑器 + 可视化面板）
    ├── parser.js           Markdown 清单解析器
    ├── useTreeRenderer.js  D3.js 树渲染组合式函数（缩放/平移/分层渲染）
    ├── defaults.js         首次加载的示例数据
    └── style.css           全局样式（设计令牌 + 布局 + 动画 + 响应式）
```

---

## 本地运行

```bash
# 1. 安装依赖
npm install

# 2. 启动开发服务器（热更新）
npm run dev

# 3. 构建生产版本（可选）
npm run build
```

构建产物输出到 `dist/` 目录，可直接部署至任何静态托管服务。
